# Multi-language REST API Examples

This directory contains autonomous examples for `https://digitca.digit.com`.

## Directories

- `rust/`
- `kotlin/`
- `flutter/`
- `nodejs/`

Each example uses defaults:

- `API_BASE=https://digitca.digit.com`
- `USERNAME=admin`
- `PASSWORD=secret`

You can override those with environment variables.

